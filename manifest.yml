name: 'netlify-plugin-npm-audit'
inputs:
  - name: level
    description: Audit level, can be 'low', 'moderate' or 'high'
    default: moderate
    required: false
  - name: skipDev
    description: Don't audit dev dependencies
    default: true
    required: false
